<Page
    x:Class="FIND_Breda.Screen.RouteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FIND_Breda.Screen"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel HorizontalAlignment="Stretch" Margin="0">
        <Grid x:Name="LayoutRoot" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="RouteLabel" Grid.Row="0" TextWrapping="Wrap" Text="Lijst van routes" FontSize="36" VerticalAlignment="Top" HorizontalAlignment="Center" FontWeight="Bold"/>
            <Button x:Name="BredaRouteButton" Grid.Row="1" Content="Breda" HorizontalAlignment="Center" Width="200" VerticalAlignment="Top" Click="BredaRouteButton_Click" Margin="0,50,0,0"/>
            
        </Grid>

    </StackPanel>
</Page>
